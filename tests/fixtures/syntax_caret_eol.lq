# Driven by scripts/test.sh: a syntax error must be attributed to the line the bad
# token is actually on, with the rendered source line matching the reported line
# (regression: a token's line once came from the lexer's running counter, not its
# start byte). The stray '3' below ends the statement-terminator expectation.
let a = 1
let b = 2 3
