# HTTP and JSON are in the language: fetch + parse in two lines, zero dependencies.
# (Only requires a network connection.)
#   loqi examples/ai/web.lq

let repo = json.parse(http.get("https://api.github.com/repos/python/cpython"))
print("{repo.full_name}: {repo.stargazers_count} stars ⭐")

# And with AI you can explain the data on the fly (requires ANTHROPIC_API_KEY):
# let summary = ai("Summarize in one sentence: {repo.description}")
# print(summary)
