# AI is first-class in Loqi: `ai(...)` calls an LLM, no SDK to install.
# Requires the ANTHROPIC_API_KEY environment variable.
#   ANTHROPIC_API_KEY=... loqi examples/ai/haiku.lq

let topic = "the sea in winter"
let poem = ai("Write a haiku about {topic}. Output only the poem.")
print(poem)
